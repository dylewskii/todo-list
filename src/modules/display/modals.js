import { allProjects } from "../..";

const modal = function() {
    const controller = {};

    const addTask = function (currentTab){
        const selectedTab = document.querySelector(".tab--active").textContent.toLowerCase();
        
    }
}